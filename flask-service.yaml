apiVersion: v1 
kind: Service 

metadata: 
  name: flask-service
  namespace: flask-project 

spec: 
  selector:
    app: flask

  ports:
    - port: 5000 
      targetPort: 5000 
      nodePort: 30081 

  type: NodePort