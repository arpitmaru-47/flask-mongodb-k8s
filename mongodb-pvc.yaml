apiVersion: v1 
kind: PersistentVolumeClaim 

metadata: 
  name: mongodb-pvc 
  namespace: flask-project 

spec:
  accessModes:
    - ReadWriteOnce 

  resources:
    requests:
      storage: 2Gi